class RetailItem:

    def main():

        def __init__(self, desc, inv, price):
            self.__desc = desc 
            self.__inv = inv
            self.__price = price
        
        def get_desc(desc):
            print(desc)

        def get_inv(inv):
            print(inv)

        def get_price(price):
            print(price)

    
